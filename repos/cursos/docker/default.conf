server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  server_tokens off;
  location / { try_files $uri $uri/ $uri.html =404; }
  location ~* \.(css|js|svg|woff2|pdf)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
