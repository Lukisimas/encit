name: encit-cursos-test
services:
  site:
    image: ${REGISTRY}/encit/cursos:${IMAGE_TAG}
    restart: unless-stopped
    networks:
      encit_control_edge:
        aliases: [cursos-test-site]
networks:
  encit_control_edge:
    external: true
